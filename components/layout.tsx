
// type IProps ={
//   icon:any,
//   title:string
// }
const Layout = (props) => {
    const { children } = props;

    return (
        <>
            {children}
        </>
    );
};

export default Layout;
